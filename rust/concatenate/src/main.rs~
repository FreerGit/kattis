use std::io;

fn main() {
    let mut input = String::new();
    io::stdin().read_line(&mut input).unwrap();
    match input.as_str().find("kth") {
        Some(at) => println!("yes"),
        None => println!("no"),
    };
}
